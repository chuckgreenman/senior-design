import Vue from 'vue';
import Vuetify from 'vuetify/lib';
import VGrid from 'vuetify/es5/components/VGrid' // Had to do this to get v-flex

Vue.use(Vuetify);
Vue.use(VGrid);

export default new Vuetify({
});
